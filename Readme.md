XML Parser and XQuery like query response 

Will eventually be released as a header/ C++ library

Features:

->Scans XML file and constructs XML tree from it
->Allows for search of tags with specific attribute in a path